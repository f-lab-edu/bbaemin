allprojects {
    group 'org.bbaemin'
    version '1.0-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'
    sourceCompatibility = JavaVersion.VERSION_11

    jar {
        enabled = true
    }
}
